syntax = "proto3";
package gc.garcol.exchange.proto;
option java_outer_classname = "TradeProto";

import "trade.command.proto";

message Command {
  oneof payload {
    NewOrder newOrder = 1;
    CancelOptionOrder cancelOptionOrder = 2;
  }
}
